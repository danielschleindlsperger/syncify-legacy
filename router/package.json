{
  "name": "router",
  "version": "0.1.0",
  "description": "Proxy server for combining front- and backend during development.",
  "main": "index.js",
  "scripts": {
    "start:dev": "node index.js",
    "docker:build": "./build.sh",
    "deploy": "kubectl --kubeconfig=\"../k8s-syncify-kubeconfig.yaml\" apply -f ./deploy.yaml"
  },
  "author": "",
  "license": "ISC"
}
