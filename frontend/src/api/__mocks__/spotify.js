export const setWebPlayerAsActiveDevice = (accessToken, deviceId) => Promise.resolve()

export const getMyPlaylists = accessToken => Promise.resolve([])

export const getPlaylist = (accessToken, playlistId) => Promise.resolve({ TODO: 'IMPLEMENT ME!' })

export const getSongs = (accessToken, songIds = []) => Promise.resolve([])
