{
  "name": "api",
  "version": "0.0.1",
  "description": "Syncify graphql backend",
  "main": "src/index.ts",
  "scripts": {
    "test": "jest",
    "build": "backpack build",
    "package-artifact": "rm -rf node_modules && npm i --production && mv node_modules build/node_modules && cd build && zip -r artifact.zip node_modules app.js app.map",
    "start": "NODE_ENV=development backpack",
    "deploy": "echo 'deploying graphql api'",
    "tsc": "tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apollo-server": "^2.9.7",
    "aws-sdk": "^2.559.0",
    "cookie": "^0.4.0",
    "jsonwebtoken": "^8.5.1",
    "source-map-support": "^0.5.13",
    "spotify-web-api-node": "^4.0.0"
  },
  "devDependencies": {
    "@types/cookie": "^0.3.3",
    "@types/jsonwebtoken": "^8.3.5",
    "@types/spotify-web-api-node": "^4.0.1",
    "backpack-core": "^0.8.4",
    "graphql-tag": "^2.10.1"
  }
}
