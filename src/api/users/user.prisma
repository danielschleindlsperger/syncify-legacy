"""
export type User = {
  id: string
  name: string
  avatar?: string
  createdAt?: string
  updatedAt?: string
}

export type AuthUser = User & {
  accessToken: string
  refreshToken: string
  deviceId?: string
}
"""

type User {
  id: ID! @id
  name: String!
  avatar: String
  createdAt: DateTime! @createdAt
  updatedAt: DateTime! @updatedAt
}
